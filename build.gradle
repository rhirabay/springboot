subprojects {
    apply plugin: 'java'
    
    ext {
        springBootVersion = '2.3.1.RELEASE'
        springCloudVersion = 'Hoxton.SR6'
        springDependencyManagementVersion = '1.0.9.RELEASE'
        benManesVersions = '0.29.0'
    }
}